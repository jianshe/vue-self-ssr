服务端渲染最终的输出成果:
1.Server Bundle  → 专门负责处理首屏渲染任务的，会生成一个html的内容（里面包括SPA应用程序的其它构成部分，让它可以在前端激活）。

2.Client Bundle  → 用来负责描述整个SPA的基本结构，把这一部分内容一起返回给前端浏览器，当页面到达前端时，前端执行一次挂载
→ 执行一个Hydrate（融合，激活）
这样一个静态的html就变成了spa应用了。

这样的话就需要两个入口文件：
1.Server-entry 专门负责处理首屏渲染。
2.Client-entry 用来激活spa应用。
